# Tracking tests

Package chứa các regression test của person tracking. Mỗi test module được đặt
trong folder riêng có README đi kèm.

```powershell
python -m unittest discover -s backend/ai_services/pose_gaze/tracking/tests -v
```
