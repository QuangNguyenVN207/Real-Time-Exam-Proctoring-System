# Holistic tests

Package chứa regression test cho landmark schema, filtering, letterbox, feature
CSV và batch dataset discovery. Mỗi test module nằm trong folder riêng có README
đi kèm.

```powershell
python -m unittest discover -s backend/ai_services/pose_gaze/holistic/tests -v
```
