# Nếu muốn dùng cpu, ko tải các gói nvidia nặng thì:
# uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu   
# uv pip install -r requirements.txt

# Còn ko thì bỏ # 3 dòng dưới và chạy uv pip install -r requirements.txt
# torch==2.13.0
# torchaudio==2.11.0
# torchvision==0.28.0
faster_whisper
numpy>=1.26,<3.0
opencv-python>=4.10,<5.0
mediapipe==0.10.33
ultralytics>=8.3,<9.0
xgboost>=2.1,<4.0
config

ctranslate2
av
rapidfuzz
silero-vad
sounddevice
scipy
onnxruntime
soundfile
librosa
numba>=0.59.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
