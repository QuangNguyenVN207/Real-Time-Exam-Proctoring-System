# Nếu muốn dùng cpu, ko tải các gói nvidia nặng thì:
# uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu   
# uv pip install -r requirements.txt

# Còn ko thì bỏ # 3 dòng dưới và chạy uv pip install -r requirements.txt
# torch==2.13.0
# torchaudio==2.11.0
# torchvision==0.28.0
faster_whisper
numpy>=1.26,<3.0
opencv-python>=4.10,<5.0
ultralytics>=8.3,<9.0
config

# face_verify: RetinaFace (detection) + ArcFace (recognition), chạy qua onnxruntime.
# Model pack "buffalo_l" (~280MB) sẽ tự động tải về ~/.insightface/models khi
# FaceVerifier khởi tạo lần đầu (cần internet).
insightface>=0.7,<1.0
# faiss-cpu: tùy chọn, tăng tốc tìm kiếm cosine similarity khi DB có nhiều sinh viên.
# uv pip install faiss-cpu


ctranslate2
av
rapidfuzz
silero-vad
sounddevice
scipy
onnxruntime
soundfile
librosa
numba>=0.59.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
